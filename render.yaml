services:
  - type: web
    name: uno-flip-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        value: <тут встав токен>
      - key: WEBHOOK_URL
        value: https://uno-flip-telegram-bot.onrender.com
